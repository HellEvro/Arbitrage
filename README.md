# Arbitrage Bot - Модульный арбитражный бот для криптовалют

Модульная асинхронная система межбиржевого спотового арбитража, работающая с биржами: **Bybit, MEXC, Bitget, OKX, KuCoin**.

Система автоматически находит общие торговые пары на всех биржах, получает актуальные цены в реальном времени и выявляет возможности арбитража с наибольшей прибылью. Включает полнофункциональный веб-интерфейс с настройками в реальном времени, фильтрацией монет, тестированием бирж и отслеживанием стабильных арбитражных возможностей.

## ✨ Основные возможности

- 🔄 **Автоматический поиск арбитража** между 5 биржами (Bybit, MEXC, Bitget, OKX, KuCoin)
- 📊 **Веб-интерфейс в реальном времени** с обновлениями через WebSocket
- ⭐ **Отслеживание стабильных возможностей** - помечает арбитражи, существующие непрерывно 5+ минут
- 🎯 **Умная фильтрация монет** - автоматически различает одинаковые и разные монеты с похожими названиями
- 🚫 **Черный и белый список** монет для гибкой фильтрации
- ⚙️ **Настройки через веб-интерфейс** - изменение параметров без перезапуска сервера
- 🔍 **Тестирование подключения к биржам** прямо из интерфейса
- 📈 **Группировка монет** по символам с возможностью сворачивания/разворачивания
- 🔗 **Прямые ссылки на биржи** для быстрого перехода к торговле
- 📱 **Telegram уведомления** о топ-возможностях (опционально)

## 🚀 Быстрый старт

### Требования

- **Python 3.11+**
- **Интернет-соединение** для доступа к API бирж
- **Браузер** для веб-интерфейса (автоматически открывается при запуске)

### Установка

#### Вариант 1: Автоматическая установка (рекомендуется)

**Windows:**
```bash
install.bat
```

**Linux/Mac:**
```bash
chmod +x install.sh
./install.sh
```

Скрипт автоматически:
- Создаст виртуальное окружение
- Установит все зависимости
- Создаст файл конфигурации из примера

#### Вариант 2: Ручная установка через requirements.txt

```bash
# Создать виртуальное окружение
python -m venv .venv

# Активировать виртуальное окружение
# На Windows:
.venv\Scripts\activate
# На Linux/Mac:
source .venv/bin/activate

# Установить зависимости
pip install -r requirements.txt

# Скопировать пример конфигурации
cp config/config.example.yaml config/config.yaml

# Отредактировать config/config.yaml (опционально)
# Настроить Telegram токен и chat_id, если нужны уведомления
```

#### Вариант 3: Использование pyproject.toml (для разработки)

```bash
# Создать виртуальное окружение
python -m venv .venv

# Активировать виртуальное окружение
# На Windows:
.venv\Scripts\activate
# На Linux/Mac:
source .venv/bin/activate

# Установить проект в режиме разработки
pip install -e ".[dev]"

# Скопировать пример конфигурации
cp config/config.example.yaml config/config.yaml
```

### Запуск

```bash
python main.py
```

После запуска:
- Система автоматически откроет браузер с веб-интерфейсом на `http://localhost:5152`
- Начнется сбор данных с бирж и поиск арбитражных возможностей
- Результаты будут отображаться в реальном времени в таблице

### Остановка

Нажмите `Ctrl+C` в терминале для корректной остановки всех компонентов.

## 🖥️ Веб-интерфейс

Веб-интерфейс предоставляет полный контроль над системой арбитража с обновлениями в реальном времени.

### Основная таблица

Главный экран показывает все арбитражные возможности, сгруппированные по монетам:

- **Группировка монет** - все возможности для одной монеты объединены в группу
- **Сворачивание/разворачивание** - клик по заголовку группы для просмотра деталей
- **Стабильные возможности** - монеты со стабильными арбитражами помечены звездочкой ⭐
- **Прямые ссылки** - клик по названию биржи открывает торговую пару на бирже
- **Цветовая индикация**:
  - Зеленая прибыль - чистая прибыль после комиссий
  - Фиолетовая валовая прибыль - прибыль до вычета комиссий
  - Красные комиссии - сумма комиссий бирж

### Панель управления

В верхней части интерфейса доступны следующие элементы управления:

#### Фильтры и сортировка

- **Показать**: выбор количества отображаемых возможностей (Топ 10/20/50/100/Все/Произвольное)
- **Сортировать по**: прибыль (USDT), прибыль (%), монета, цена покупки, цена продажи
- **Поиск**: быстрый поиск по символу монеты
- **Зафиксировать**: остановка обновления данных для анализа текущего состояния

#### Управление группами

- **Кнопка свернуть/развернуть все** (▼) - быстрое сворачивание или разворачивание всех групп монет

### Статус бирж

В верхней части интерфейса отображается панель статуса подключения к биржам:

- **Статус подключения** - показывает количество загруженных монет и время последнего обновления
- **Индикаторы**:
  - ✅ Зеленая галочка - биржа подключена и работает
  - ❌ Красный крестик - биржа недоступна или не подключена
- **Тестирование** - клик по недоступной бирже запускает тест подключения с подробными результатами

### Настройки

Вкладка "Настройки" содержит три раздела:

#### 1. Параметры расчета прибыли

Настройки, влияющие на расчет прибыли от арбитража:

- **Notional USDT** - сумма в USDT, используемая для расчета прибыли (по умолчанию: 1000)
- **Slippage (базовые пункты)** - проскальзывание при исполнении ордеров (по умолчанию: 3.0)
- **Минимальная прибыль (USDT)** - минимальная чистая прибыль для отображения (по умолчанию: 0.5)
- **Минимальный спред (%)** - минимальный процентный спред для отображения (по умолчанию: 0.05)

**Важно**: Изменения сохраняются в `config.yaml` и применяются автоматически без перезапуска сервера.

#### 2. Параметры фильтрации монет

Настройки для определения одинаковых и разных монет на разных биржах:

**Backend (серверная фильтрация):**
- **Same Coin Ratio** - порог для идентичных названий (по умолчанию: 1.10 = 10% разница)
- **Likely Same Coin Ratio** - порог для похожих названий (по умолчанию: 1.5 = 50% разница)
- **Different Coin Ratio** - порог для разделения разных монет (по умолчанию: 1.5 = 50% разница)
- **Min Price Threshold** - минимальный порог цены для определения нулевых значений (по умолчанию: 1e-6)
- **Price Ratio Threshold** - максимальное соотношение цен для одной монеты (по умолчанию: 1.5)
- **Stable Window (минуты)** - временное окно для определения стабильных арбитражей (по умолчанию: 5.0)

**Frontend (группировка на странице):**
- **Price Diff Suspicious** - порог подозрительной разницы цен (по умолчанию: 0.3 = 30%)
- **Price Diff Threshold** - порог для определения разных монет (по умолчанию: 1.0 = 100%)
- **Price Diff Aggressive** - агрессивный порог разделения (по умолчанию: 2.0 = 200%)

**Важно**: Изменения сохраняются в `config.yaml` и применяются автоматически без перезапуска сервера.

#### 3. Настройки отображения

- **Использовать черный список** - включить/выключить фильтрацию по черному списку
- **Использовать белый список** - включить/выключить фильтрацию по белому списку (приоритет)
- **Автоматически сортировать белый список вверху** - монеты из белого списка всегда отображаются первыми

### Черный список монет

Монеты из черного списка полностью исключаются из отображения в основной таблице.

**Использование:**
1. Введите символ монеты (например, `BTCUSDT` или `BTC`)
2. Нажмите "Добавить" или Enter
3. Монета будет добавлена в черный список и скрыта из таблицы
4. Для удаления нажмите "Удалить" рядом с монетой

**Особенности:**
- Поддерживает различные форматы символов (`BTC`, `BTCUSDT`, `BTC-USDT`)
- Фильтрация работает по базовому активу (например, `BTC` скроет все пары с BTC)
- Список сохраняется в localStorage браузера

### Белый список монет

Монеты из белого списка отображаются в приоритете (вверху таблицы) или только они (если включен режим "только белый список").

**Использование:**
1. Введите символ монеты (например, `ETHUSDT` или `ETH`)
2. Нажмите "Добавить" или Enter
3. Монета будет добавлена в белый список
4. При включенной опции "Автоматически сортировать белый список вверху" монеты из белого списка всегда будут в начале таблицы

**Особенности:**
- Поддерживает различные форматы символов
- Фильтрация работает по базовому активу
- Список сохраняется в localStorage браузера

### Стабильные арбитражные возможности

Система отслеживает стабильность арбитражных возможностей:

- **Звездочка ⭐** отображается справа от названия монеты в заголовке группы, если в группе есть хотя бы одна стабильная возможность
- **Стабильная возможность** - арбитраж, существующий непрерывно в течение заданного времени (по умолчанию: 5 минут)
- **Визуальная индикация** - стабильные возможности имеют зеленоватый фон в таблице
- **Tooltip** - при наведении на звездочку показывается информация о стабильности

Стабильные арбитражи имеют более высокую вероятность успешного выполнения, так как разница в ценах сохраняется длительное время, что позволяет успеть перебросить монеты между биржами.

### Тестирование бирж

Для проверки подключения к бирже:

1. Найдите биржу в панели статуса с красным крестиком ❌
2. Кликните по названию биржи
3. Откроется модальное окно с результатами теста:
   - Проверка подключения к API
   - Получение списка рынков
   - Получение котировок
   - Детальная информация об ошибках (если есть)

Это помогает быстро диагностировать проблемы с подключением к конкретной бирже.

## 📋 Конфигурация

Основной файл конфигурации: `config/config.yaml`

### Основные настройки

```yaml
# Список бирж для мониторинга
exchanges:
  - bybit
  - mexc
  - bitget
  - okx
  - kucoin

# Размер позиции по умолчанию (USDT)
notional_usdt_default: 1000

# Комиссии бирж (можно переопределить через API)
fees:
  bybit:
    taker: 0.001
    maker: 0.001
  mexc:
    taker: 0.001
    maker: 0.001
  bitget:
    taker: 0.001
    maker: 0.001
  okx:
    taker: 0.001
    maker: 0.001
  kucoin:
    taker: 0.001
    maker: 0.001

# Проскальзывание (базовые пункты)
slippage_bps: 3

# Пороги для фильтрации возможностей
thresholds:
  min_profit_usdt: 0.5      # Минимальная прибыль в USDT
  min_spread_pct: 0.05      # Минимальный спред в процентах
  stale_ms: 1500            # Максимальный возраст данных (мс)

# Параметры фильтрации монет
filtering:
  # Пороги для определения одной монеты vs разных монет (backend)
  same_coin_ratio: 1.10              # Разница до 10% - это одна монета (для идентичных названий)
  likely_same_coin_ratio: 1.5       # Разница до 50% - скорее всего одна монета (для разных длин названий)
  different_coin_ratio: 1.5         # Разница >= 1.5x - точно разные монеты
  
  # Пороги для фильтрации арбитражных возможностей (backend)
  min_price_threshold: 1e-6          # Цены меньше этого считаем практически нулевыми
  price_ratio_threshold: 1.5        # Если цена в N раз больше - это разные монеты
  
  # Окно стабильности (backend)
  stable_window_minutes: 5.0        # Окно стабильности в минутах для пометки стабильных арбитражей
  
  # Пороги для группировки на фронтенде
  price_diff_suspicious: 0.3        # 30% - подозрительная разница
  price_diff_threshold: 1.0         # 100% (в 2 раза) - точно разные монеты
  price_diff_aggressive: 2.0        # 200% (в 3 раза) - агрессивное разделение

# Веб-интерфейс
web:
  host: "0.0.0.0"
  port: 5152
  cors_origins:
    - "*"

# Telegram уведомления (опционально)
telegram:
  enabled: true
  bot_token: "<YOUR_TOKEN>"      # Токен бота от @BotFather
  chat_id: "<YOUR_CHAT_ID>"      # ID чата (можно получить у @userinfobot)
  notify_interval_sec: 60
  min_profit_usdt: 1.0

# Логирование
logging:
  level: "INFO"                  # DEBUG, INFO, WARNING, ERROR
  json: false                     # JSON формат логов
```

### Настройка Telegram уведомлений

1. Создайте бота через [@BotFather](https://t.me/BotFather) в Telegram
2. Получите токен бота
3. Узнайте свой chat_id через [@userinfobot](https://t.me/userinfobot)
4. Вставьте значения в `config/config.yaml`:
   ```yaml
   telegram:
     enabled: true
     bot_token: "1234567890:ABCdefGHIjklMNOpqrsTUVwxyz"
     chat_id: "123456789"
   ```

### Изменение настроек через веб-интерфейс

Большинство параметров можно изменить прямо в веб-интерфейсе:

1. Откройте вкладку "Настройки"
2. Измените нужные параметры
3. Нажмите "Сохранить" в соответствующем разделе
4. Изменения автоматически сохраняются в `config.yaml` и применяются без перезапуска сервера

**Доступные для изменения через UI:**
- Параметры расчета прибыли (notional_usdt_default, slippage_bps, min_profit_usdt, min_spread_pct)
- Параметры фильтрации монет (все параметры из раздела `filtering`)

## 🏗️ Архитектура

Система построена по модульному принципу, что позволяет легко добавлять новые биржи и функциональность.

### Основные компоненты

#### 1. **MarketDiscovery** (`arbitrage_bot/services/market_discovery.py`)
- Находит пересечение спотовых рынков между всеми биржами
- Определяет общие торговые пары (USDT)
- Требует минимум 2 биржи для работы арбитража
- Автоматически обновляет список рынков
- Умное сопоставление символов между биржами (например, `ZKSYNCUSDT` на Bitget → `ZKUSDT`)

#### 2. **ExchangeAdapters** (`arbitrage_bot/exchanges/`)
Унифицированные адаптеры для каждой биржи:
- **BybitAdapter** - использует REST API `/v5/market/tickers`
- **MexcAdapter** - использует REST API `/api/v3/ticker/24hr` (с поддержкой rate limiting)
- **BitgetAdapter** - использует REST API `/api/spot/v1/market/tickers`
- **OkxAdapter** - использует REST API `/api/v5/market/tickers`
- **KucoinAdapter** - использует REST API `/api/v1/market/allTickers`

Все адаптеры:
- Работают с публичными API (без API ключей)
- Получают данные о рынках и котировках
- Обрабатывают ошибки и автоматически переподключаются
- Поддерживают минимальные требования (2+ биржи)
- Фильтруют только спотовые рынки

#### 3. **QuoteAggregator** (`arbitrage_bot/services/quote_aggregator.py`)
- Агрегирует котировки от всех бирж
- Хранит данные в памяти (`QuoteStore`)
- Автоматически переподключается при сбоях
- Использует экспоненциальную задержку при ошибках
- Producer-consumer архитектура для эффективной обработки

#### 4. **ArbitrageEngine** (`arbitrage_bot/services/arbitrage_engine.py`)
- Вычисляет спреды и потенциальную прибыль
- Учитывает комиссии бирж (автоматически получает с API или использует настройки)
- Учитывает проскальзывание (slippage)
- Фильтрует возможности по порогам
- Отслеживает стабильность арбитражных возможностей
- Умная фильтрация монет - различает одинаковые и разные монеты с похожими названиями
- Возвращает все возможности (фильтрация на клиенте)

#### 5. **FeeFetcher** (`arbitrage_bot/services/fee_fetcher.py`)
- Автоматически получает реальные комиссии с бирж
- Поддерживает символ-специфичные комиссии (например, MEXC)
- Кэширует данные для оптимизации

#### 6. **WebUI** (`arbitrage_bot/web/`)
- Flask веб-сервер на порту 5152
- SocketIO для обновлений в реальном времени
- Таблица рейтинга арбитражных возможностей с группировкой
- Настройки через веб-интерфейс с hot-reload
- Черный и белый список монет
- Тестирование подключения к биржам
- Статус бирж в реальном времени
- Sticky header таблицы при прокрутке

#### 7. **TelegramNotifier** (`arbitrage_bot/services/telegram_notifier.py`)
- Отправляет уведомления о топ-возможностях
- Настраиваемый интервал отправки
- Фильтрация по минимальной прибыли

### Структура проекта

```
arbitrage_bot/
├── config/              # Конфигурация (загрузка, модели)
├── core/                # Базовые утилиты (HTTP, логирование, исключения)
├── exchanges/           # Адаптеры бирж
│   ├── base.py         # Базовый класс адаптера
│   ├── bybit.py        # Bybit адаптер
│   ├── mexc.py         # MEXC адаптер
│   ├── bitget.py       # Bitget адаптер
│   ├── okx.py          # OKX адаптер
│   └── kucoin.py       # KuCoin адаптер
├── services/            # Основные сервисы
│   ├── market_discovery.py    # Поиск общих рынков
│   ├── quote_store.py         # Хранилище котировок
│   ├── quote_aggregator.py    # Агрегатор котировок
│   ├── arbitrage_engine.py    # Движок арбитража
│   ├── fee_fetcher.py         # Получение комиссий
│   └── telegram_notifier.py  # Telegram уведомления
└── web/                 # Веб-интерфейс
    ├── app.py           # Flask приложение
    ├── templates/       # HTML шаблоны
    └── static/          # CSS и JavaScript

config/
└── config.example.yaml  # Пример конфигурации

logs/                    # Логи (создается автоматически)
├── system.log          # Системные логи
├── bybit.log           # Логи Bybit адаптера
├── mexc.log            # Логи MEXC адаптера
├── bitget.log          # Логи Bitget адаптера
├── okx.log             # Логи OKX адаптера
├── kucoin.log          # Логи KuCoin адаптера
└── ...                 # Логи других модулей
```

## 🔧 Технологии

- **Python 3.11+** - основной язык
- **asyncio** - асинхронное программирование
- **aiohttp** - асинхронные HTTP запросы
- **websockets** - поддержка WebSocket (для будущих улучшений)
- **Flask + Flask-SocketIO** - веб-интерфейс с обновлениями в реальном времени
- **pydantic** - валидация конфигурации
- **structlog** - структурированное логирование
- **aiogram** - Telegram бот для уведомлений
- **PyYAML** - парсинг конфигурации

## 📊 Логирование

Система использует детальное логирование с ротацией файлов:

- **Отдельный лог-файл для каждого модуля** (10 МБ, 5 бэкапов)
- **Консольный вывод** для мониторинга в реальном времени
- **Логи хранятся в `logs/`** директории

### Уровни логирования

- `DEBUG` - детальная отладочная информация
- `INFO` - общая информация о работе системы
- `WARNING` - предупреждения (например, недоступность биржи)
- `ERROR` - ошибки, требующие внимания

## 🐳 Docker

### Запуск через Docker Compose

```bash
docker compose up --build
```

Конфигурация монтируется из `config/config.yaml` (создайте файл на основе `config.example.yaml`).

## 🔍 Мониторинг и отладка

### Проверка работы системы

1. **Веб-интерфейс**: Откройте `http://localhost:5152` в браузере
2. **Логи**: Проверьте файлы в `logs/` директории
3. **API**: Проверьте `http://localhost:5152/api/ranking` для получения данных в JSON
4. **Статус**: Проверьте `http://localhost:5152/api/status` для проверки работы сервера

### Типичные проблемы

#### Биржа не возвращает данные (403 ошибки)
- **MEXC**: Использует rate limiting, система автоматически применяет экспоненциальную задержку
- Система продолжит работать с другими биржами (минимум 2 требуются)
- Используйте функцию тестирования биржи в веб-интерфейсе для диагностики

#### Пустая таблица в веб-интерфейсе
- Подождите 10-20 секунд после запуска для сбора данных
- Проверьте логи в `logs/` для диагностики
- Убедитесь, что минимум 2 биржи работают
- Проверьте статус бирж в верхней панели интерфейса

#### Socket.IO не подключается
- Проверьте настройки CORS в `config/config.yaml`
- Убедитесь, что порт 5152 не занят другим процессом
- Система автоматически освобождает порт при запуске

#### Монеты с одинаковыми названиями группируются неправильно
- Настройте параметры фильтрации в веб-интерфейсе (вкладка "Настройки")
- Уменьшите `same_coin_ratio` для более строгой фильтрации
- Увеличьте `price_ratio_threshold` для разделения разных монет
- Используйте черный список для исключения проблемных монет

## 🧪 Разработка

### Установка зависимостей для разработки

```bash
pip install -r requirements-dev.txt
# или
pip install -e ".[dev]"
```

### Запуск тестов

```bash
pytest
# или
make test
```

### Линтинг и форматирование

```bash
ruff check .
ruff format .
# или
make lint
```

### Проверка типов

```bash
mypy arbitrage_bot
```

## 📝 Особенности реализации

### Автоматическое получение комиссий
Система автоматически получает реальные комиссии с бирж через API, не требуя ручного ввода в конфигурации. При недоступности API используются значения из конфигурации.

### Устойчивость к сбоям
- Система продолжает работать даже если некоторые биржи недоступны (минимум 2 требуются)
- Автоматическое переподключение при ошибках
- Экспоненциальная задержка при повторных попытках
- Graceful degradation - система работает с доступными биржами

### Оптимизация производительности
- Асинхронная обработка всех запросов
- Кэширование комиссий
- In-memory хранилище котировок для быстрого доступа
- Producer-consumer архитектура для эффективной обработки котировок
- Atomic dictionary switching для неблокирующего чтения данных

### Умная фильтрация монет
Система автоматически различает одинаковые и разные монеты с похожими названиями:
- **Идентичные названия** (например, BTC на всех биржах) - строгая проверка разницы цен
- **Похожие названия** (например, ZKSYNC и ZK) - более мягкая проверка с учетом арбитражных погрешностей
- **Разные монеты** - автоматическое разделение при большой разнице цен
- **Нулевые цены** - фильтрация "мертвых" монет и некорректных данных

### Стабильность арбитражных возможностей
Система отслеживает, как долго арбитражная возможность существует непрерывно:
- История временных меток для каждой пары (монета, биржа покупки, биржа продажи)
- Автоматическая очистка старых записей
- Пометка стабильных возможностей звездочкой ⭐
- Настраиваемое временное окно (по умолчанию: 5 минут)

### Hot-reload настроек
Изменения параметров через веб-интерфейс:
- Автоматически сохраняются в `config.yaml`
- Применяются к `ArbitrageEngine` без перезапуска сервера
- Не требуют перезагрузки страницы

### Безопасность
- Все запросы к биржам используют публичные API (без API ключей)
- Конфигурация с чувствительными данными (Telegram токены) хранится локально
- Автоматическая очистка порта перед запуском
- CORS настраивается через конфигурацию

## 📚 Дополнительная документация

- [Техническое задание](docs/arbitrage_bot_TZ.md) - полное описание требований и архитектуры
- [Пример конфигурации](config/config.example.yaml) - все доступные настройки

## ⚠️ Важные замечания

1. **API ключи не требуются** - система использует только публичные endpoints для получения данных о ценах
2. **Минимум 2 биржи** - система требует минимум 2 работающие биржи для поиска арбитража
3. **Автоматическая очистка порта** - при запуске система автоматически завершает предыдущие процессы на порту 5152
4. **Логи ротируются** - каждый лог-файл ограничен 10 МБ с 5 бэкапами
5. **Только спотовые рынки** - система работает только со спотовыми торговыми парами
6. **USDT пары** - система ищет арбитраж только между парами с USDT в качестве котируемой валюты
7. **Стабильность не гарантирует прибыль** - стабильные арбитражи имеют более высокую вероятность успешного выполнения, но не гарантируют прибыль

## 🎯 Использование

### Базовое использование

1. Запустите систему: `python main.py`
2. Дождитесь загрузки данных (10-20 секунд)
3. Просмотрите арбитражные возможности в таблице
4. Используйте фильтры и сортировку для поиска лучших возможностей
5. Кликните по названию биржи для перехода к торговле

### Продвинутое использование

1. **Настройка фильтрации**:
   - Откройте вкладку "Настройки"
   - Настройте параметры фильтрации монет под ваши нужды
   - Сохраните изменения

2. **Использование черного/белого списка**:
   - Добавьте нежелательные монеты в черный список
   - Добавьте интересующие монеты в белый список для приоритетного отображения

3. **Отслеживание стабильных арбитражей**:
   - Ищите монеты со звездочкой ⭐
   - Эти арбитражи существуют длительное время и имеют более высокую вероятность успешного выполнения

4. **Тестирование бирж**:
   - Используйте функцию тестирования для диагностики проблем с подключением
   - Проверяйте статус бирж в верхней панели

5. **Фиксация данных**:
   - Используйте кнопку "Зафиксировать" для остановки обновлений
   - Полезно для анализа конкретного момента времени

## 📄 Лицензия

MIT

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи в `logs/` директории
2. Убедитесь, что конфигурация корректна
3. Проверьте доступность бирж через браузер
4. Используйте функцию тестирования бирж в веб-интерфейсе
5. Проверьте статус бирж в верхней панели интерфейса

---

**Версия:** 0.2.0  
**Последнее обновление:** 2025-11-09
